import React from "react";

import SpaceshipInteriorFront from "./spaceship/interior-front/SpaceshipInteriorFront";

const Canvas = props => (
  <svg width="800" height="600">
    <SpaceshipInteriorFront />
  </svg>
);
export default Canvas;
